apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceest-app-green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aceest
      version: green
  template:
    metadata:
      labels:
        app: aceest
        version: green
    spec:
      containers:
      - name: aceest-container
        image: ankit2024tm93604/aceest-app:latest
        ports:
        - containerPort: 5000